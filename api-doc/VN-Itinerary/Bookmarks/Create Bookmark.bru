meta {
  name: Create Bookmark
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/v1/bookmarks
  body: json
  auth: bearer
}

auth:bearer {
  token: {{accessToken}}
}

body:json {
  {
    "itineraryId": "uuid-here"
  }
}
